---
title: Beta
description: A page titled by frontmatter alone, so the synthetic-title path is built too.
---

*A page titled by frontmatter alone, so the synthetic-title path is built too.*

## Why it exists

Alpha writes its own `# Alpha`; this page does not. Both must carry the Created line under the title.
